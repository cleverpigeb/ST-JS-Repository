/** 装备槽位常量。**技能表本体已不在这里。**
 *
 * 2026-09-14 裁定（草案 §6.1 line 268）把技能表整张搬进 MVU 的根级 `$技能表`，
 * 面板可编、模型不可写。本文件原先持有的 `SkillSpec`／`SKILLS`／`HER_SKILL_SET` 与
 * `lookupSkill`／`skillOwner`／`herSkillSet` 全部退役，读法改由 `logic/skill-table.ts`
 * 从变量里取——那套静态表正是用户实测第五条「编辑状态无法更改技能的描述与数值」的成因：
 * 数值在这里、叙事指导在世界书 `战斗叙事对照表` 正文里，编辑态碰不到任何一半。
 *
 * **不要把数值再抄回本文件。** 抄回来就又是两份真源，而 `战斗叙事对照表` 那条已经改成
 * 从 `$技能表` 用 EJS 渲染，静态副本一改就和正文对不上。
 */

/** 装备槽恒 4，只替换不增减，只能在战斗外更换（schema `主角.装备四槽`／草案 §12.2）。
 * 这一条是真·静态：槽位数是机制常量，不随剧情变，也不进变量。 */
export const EQUIP_SLOTS = ['槽1', '槽2', '槽3', '槽4'] as const;
