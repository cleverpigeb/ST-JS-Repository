import type { Schema } from '../../schema';

/** 七段关系。取值直接从 schema 派生，改 schema 时这里会跟着报错而不是静默错位。 */
export type Stage = Schema['关系']['_当前关系'];

/** 逐级 ±1 不跳段（草案 §1）。常显头部的关系单选按这个顺序排。 */
export const STAGE_ORDER = ['同学', '朋友', '暧昧', '恋人', '亲密恋人', '订婚', '结婚'] as const satisfies readonly Stage[];

/** 双方生命／精力上限（草案 §5 数值模型，逐字照抄）。
 *
 * 她的两项是**派生只读**，脚本按 `关系.$已达最高关系阶段` 查本表写入 `朱小笋.$生命上限`／`$精力上限`；
 * 面板只显示、不给编辑入口。主角两项是持久可编字段，本表的 `主角生命`／`主角精力` 只作为
 * 「该档位的参考值」用于战斗态开战置满与面板提示，不反向改写 `主角.生命上限`。
 *
 * 表内关系：主角生命上限 ＝ 她的同档 ÷ 1.2 四舍五入到 5 的倍数；
 * 双方精力上限 ＝ 各自生命上限 ÷ 2 向下取整到 5 的倍数。
 */
export const STAGE_CAPS: Record<Stage, { 她生命: number; 她精力: number; 主角生命: number; 主角精力: number }> = {
  同学: { 她生命: 60, 她精力: 30, 主角生命: 50, 主角精力: 25 },
  朋友: { 她生命: 80, 她精力: 40, 主角生命: 65, 主角精力: 30 },
  暧昧: { 她生命: 100, 她精力: 50, 主角生命: 85, 主角精力: 40 },
  恋人: { 她生命: 120, 她精力: 60, 主角生命: 100, 主角精力: 50 },
  亲密恋人: { 她生命: 160, 她精力: 80, 主角生命: 135, 主角精力: 65 },
  订婚: { 她生命: 200, 她精力: 100, 主角生命: 165, 主角精力: 80 },
  结婚: { 她生命: 240, 她精力: 120, 主角生命: 200, 主角精力: 100 },
};

/** 主角两项上限的**面板下限**＝该开局的初始值（草案 §12.4 恋人开局 100／50）。
 * 剧情层唯一的抬升渠道是专属任务，schema 侧不夹取，由前端提交时把关（§5.8 字段落位表）。 */
export const PROTAGONIST_FLOOR = { 生命上限: 100, 精力上限: 50 } as const;

/** 每回合精力恢复 ＝ 该方精力上限的 20%（草案 §5）。回合结束面板要显示这一项。 */
export const EP_RECOVER_RATIO = 0.2;

/** 偏差修正四档（草案 §10.1）。质变决斗恒 ×1.0，普通请愿决斗按偏差档改写。
 * schema 侧用「就近吸附到四档」的 transform 收口，所以面板只需提供这四个选项。 */
export const MULTIPLIER_TIERS = [
  { value: 1.0, label: '×1.0', hint: '偏差 0 档／质变决斗恒取此档' },
  { value: 1.5, label: '×1.5', hint: '偏差 1 档' },
  { value: 2.5, label: '×2.5', hint: '偏差 2 档' },
  { value: 4.0, label: '×4.0', hint: '偏差 3 档' },
] as const;

/** 暴击倍率全局固定 1.5×（草案 §6.2 硬约束）。 */
export const CRIT_MULTIPLIER = 1.5;
